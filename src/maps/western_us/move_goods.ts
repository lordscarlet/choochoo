import { MoveAction } from "../../engine/move/move";


export class WesternUsMoveAction extends MoveAction {
  // TODO: figure out how to let them choose which railroad to use.
  // process(action: MoveData): boolean {
  //   const result = super.process(action);

  //   const startingLocation = this.grid().get(action.startingCity);
  //   if (action.starting)
  // }
}