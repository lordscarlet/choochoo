import {Coordinates} from "../../utils/coordinates";

export const PORT_CITIES: Array<Coordinates> = [
    Coordinates.from({q: 7, r: 4}),
    Coordinates.from({q: 0, r: 12}),
    Coordinates.from({q: 0, r: 15}),
    Coordinates.from({q: 0, r: 18}),
    Coordinates.from({q: 0, r: 21}),
    Coordinates.from({q: 12, r: 6}),
]
